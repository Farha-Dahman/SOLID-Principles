package edu.najah.S.implt;

import edu.najah.S.Invoice;
import edu.najah.S.intrf.IaddInvoice;

public class addInvoice implements IaddInvoice {
    public void addOnDB(Invoice invoice) {
        // connect to database
        // execute insert command to add new record in database
        //close the database connection
    }
}
